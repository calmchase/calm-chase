// import { DB, DU, HOST, PW } from '$env/static/private';
// import { drizzle } from 'drizzle-orm/postgres-js';
// import postgres from 'postgres';
// import * as schema from './schema';

// export const db = drizzle(
// 	postgres({
// 		host: `${HOST}`,
// 		user: `${DU}`,
// 		database: `${DB}`,
// 		password: `${PW}`
// 	}),
// 	{ schema }
// );
